# 測試檢查清單

## 自動檢查（CI，push 到 main 時自動執行，見 `.github/workflows/deploy.yml`）

1. **必要檔案存在檢查**：確認 `index.html` 存在且非空
2. **JS 語法檢查**：從 `index.html` 擷取所有 `<script>` 內容，用 `node --check` 驗證語法正確——語法錯誤會讓整頁失效卻不會顯示任何錯誤訊息，是部署前最重要的一道關卡

以上任一項失敗，部署會中止，不會把壞掉的版本發布到線上。

## 手動測試（每次改動後，實際在瀏覽器操作一次）

### 設定頁
- [ ] 修改左右隊名，畫面即時反映
- [ ] 題庫少於 4 題時「開始遊戲」被停用並顯示警告文字
- [ ] 切換題數／秒數選單，重新整理頁面後設定仍保留（localStorage）

### 題庫管理
- [ ] 新增一題、編輯內容、選擇正解、刪除該題，畫面與題數即時同步
- [ ] 貼上 Excel 格式文字後按「新增到題庫」，正確解析出題目筆數；格式錯誤的列有被略過並提示筆數
- [ ] 「取代整個題庫」有二次確認，確認後題庫確實被取代
- [ ] 「複製題庫到 Excel」按下後文字框出現內容，貼回 Excel 欄位對齊正確
- [ ] 「還原範例題庫」有二次確認，確認後恢復成預設 10 題

### 遊戲畫面
- [ ] 兩隊左右並排顯示；切換到手機寬度（瀏覽器開發者工具的裝置模擬）仍維持左右兩欄，不跑版、不橫向捲動
- [ ] 倒數時間到時自動判定未作答並進入下一題，計時條顏色依剩餘時間變化（綠→橘→紅）
- [ ] 點選正確答案得分並即時顯示在分數欄；點選錯誤答案不加分且會標示正解
- [ ] 一隊先完成全部題目時顯示「已完成」，等待另一隊
- [ ] 兩隊都完成後自動跳到結果頁；「結束遊戲」可提前強制結束（有二次確認）

### 結果頁
- [ ] 長條圖分數與隊名正確對應，答對題數／總題數正確
- [ ] 分數相同時顯示平手訊息，不同時正確標示獲勝隊伍
- [ ] 「再玩一次」用同樣設定重新開始一場新遊戲；「回到設定」正確返回設定頁並保留原本輸入

## 部署後驗證

- [ ] 開啟 `https://<你的帳號>.github.io/quiz-arena/`，確認畫面正常載入（無空白頁、無主控台錯誤）
- [ ] 剛部署完成後 GitHub Pages 建置可能需要 1-2 分鐘；若頁面沒更新，先強制重新整理（Mac：Cmd+Shift+R）
- [ ] 到 repo 的 Actions 分頁確認最新一次 workflow 顯示綠色勾勾（成功）
