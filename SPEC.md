# 問答擂台（Quiz Arena）規格書

## 專案目的

課堂分組問答活動用的網頁小遊戲。左右兩隊（隊名可自訂）在同一畫面上並排，各自獨立進行限時問答，全部答完後自動計分並顯示勝負結果。純前端單檔網頁，不需要伺服器或建置工具，適合 GitHub Pages 這類靜態託管。

## 檔案結構

```
quiz-arena/
├── index.html                     # 整個遊戲（HTML/CSS/JS 全部內嵌在單一檔案）
├── SPEC.md                        # 本文件
├── TESTING.md                     # 測試檢查清單
├── README.md                      # 專案總覽與連結
└── .github/workflows/deploy.yml   # GitHub Actions：push 到 main 時自動測試並部署
```

## 遊戲流程

### 1. 設定頁（Setup）
- 輸入左右兩隊隊名（預設「第一隊」／「第二隊」，最長 12 字）
- 選擇每題作答時間：10 / 15 / 20（預設）/ 30 秒
- 選擇本場題數：5 / 10（預設）/ 15 / 20 / 全部題目
- 顯示目前題庫題數；題庫少於 4 題時停用「開始遊戲」並顯示警告
- 「題庫管理」按鈕進入題庫管理頁

### 2. 題庫管理頁（Editor）
- 表格逐題編輯：題目、選項 A-D、正解（下拉選單）、刪除
- 「新增一題」新增空白列
- 「還原範例題庫」以內建 10 題範例題目取代整個題庫（需二次確認）
- 「從 Excel 貼上匯入」：貼上 Tab 或逗號分隔文字（題目、選項A-D、正解），可選「新增到題庫」或「取代整個題庫」；正解可填 1-4 或 A-D；格式不符的列會被略過並顯示略過筆數
- 「複製題庫到 Excel」：產生 TSV 文字，嘗試寫入剪貼簿，並顯示於文字框供手動全選複製
- 題庫與設定存在瀏覽器 `localStorage`（key：`qaArena.bank.v1`、`qaArena.settings.v1`），只存在該瀏覽器裝置上，換瀏覽器或清除資料會遺失

### 3. 遊戲畫面（Game）
- 畫面固定左右兩欄，不因螢幕縮小而變成上下堆疊，只會縮小字體與間距（手機寬度 ~400px 仍維持左右並排）
- 每隊獨立進度：題目順序、四個選項順序皆各自獨立隨機重排
- 每題倒數計時：時間到自動判定未作答（不加分）；剩餘時間低於 50% 計時條轉橘色、低於 20% 轉紅色
- 答對 +10 分，作答後立即顯示正解／答錯反饋，1.1 秒後自動進入下一題
- 一隊先完成全部題目會顯示「已完成」並等待另一隊；兩隊皆完成後自動進入結果頁
- 右上角「結束遊戲」可隨時提前結束（需二次確認）

### 4. 結果頁（Results）
- SVG 長條圖比較兩隊分數，並列出各隊答對題數／總題數
- 勝負判定：分數高者獲勝，相同則平手
- 「再玩一次（同樣設定）」重新洗牌並用同樣設定開始；「回到設定」返回設定頁

## 技術重點

- 純前端單檔 HTML，無外部框架、無建置流程
- 深色「擂台」視覺主題（固定深色，不隨系統淺色模式切換）：隊伍 A 藍色 `#3987e5`、隊伍 B 橘色 `#d95926`
- 中文字型 Noto Sans TC；分數／計時數字用等寬字 JetBrains Mono，皆由 Google Fonts 載入
- 響應式版面：側邊留白至少 16px，手機寬度下不會出現橫向捲動

## 已知限制

- 題庫僅存在單一瀏覽器的 `localStorage`，不會跨裝置同步；要在多台裝置共用同一題庫，需手動用「複製題庫」／「貼上匯入」搬移
- 目前僅支援兩隊、四選一單選題型；如需更多隊伍或是非題、簡答題，需另外擴充
